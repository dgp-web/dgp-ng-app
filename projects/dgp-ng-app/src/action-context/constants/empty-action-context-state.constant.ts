import { ActionContextState } from "../models/action-context-state.model";
import { emptyActionContext } from "./empty-action-context.constant";

export const emptyActionContextState: ActionContextState = {
    selectedActionContext: emptyActionContext
};

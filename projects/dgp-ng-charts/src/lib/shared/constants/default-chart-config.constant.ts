import { SharedChartConfig } from "dgp-ng-charts";

export const defaultChartConfig: SharedChartConfig = {
    margin: {
        top: 10,
        right: 30,
        left: 50,
        bottom: 20
    }
};
